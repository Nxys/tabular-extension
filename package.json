{
  "name": "browser-selection-copy",
  "version": "1.0.0",
  "description": "浏览器框选复制插件 - Chrome扩展",
  "type": "module",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf build/dist",
    "build": "npm run clean && tsc --outDir build/dist",
    "icon": "node build/icon.cjs",
    "extension": "npm run icon && node build/extension.cjs",
    "release": "npm run lint && npm run test && npm run build && npm run extension",
    "zip": "node build/zip.cjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/jest": "^29.5.8",
    "@types/jsdom": "^21.1.6",
    "@types/sharp": "^0.32.0",
    "@typescript-eslint/eslint-plugin": "^6.12.0",
    "@typescript-eslint/parser": "^6.12.0",
    "eslint": "^8.54.0",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "sharp": "^0.33.5",
    "ts-jest": "^29.1.1",
    "typescript": "~5.3.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/test/setup.ts"
    ],
    "moduleNameMapper": {
      "^(\\.{1,2}/.*)\\.js$": "$1"
    },
    "transform": {
      "^.+\\.tsx?$": [
        "ts-jest",
        {
          "tsconfig": "tsconfig.test.json"
        }
      ]
    },
    "testMatch": [
      "**/test/**/*.test.ts"
    ]
  }
}
