# 实施计划：更新架构规范和文档

## 概述

本实施计划将新的插件开发规范更新到项目中，包括更新 steering 文件、README、STRUCTURE.md 文档，验证代码结构和实现的一致性，并生成架构一致性报告。

实施策略：
1. 先更新文档（steering、README、STRUCTURE.md）
2. 再验证代码结构和实现
3. 最后生成架构一致性报告

## 任务

- [x] 1. 更新 Steering 文件
  - 将新规范的完整内容更新到 `.kiro/steering/architecture-structure.md`
  - 包含：整体设计原则、模块职责划分、样式与 UI 设计约定、目录结构、通信与状态设计、工程与构建层共识、被否定的设计方向
  - 确保格式清晰，使用 Markdown 标题和列表
  - 保留 front-matter（`inclusion: always`）
  - _需求：1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. 更新 README 文档
  - [x] 2.1 更新项目结构部分
    - 确保目录树与新规范定义的结构一致
    - 更新各目录的说明文字
    - _需求：2.1_

  - [x] 2.2 更新核心架构部分
    - 更新架构原则说明（Background、Content、Shared 层）
    - 确保与新规范的设计原则一致
    - _需求：2.2_

  - [x] 2.3 更新核心组件部分
    - 更新各层文件的职责说明
    - 确保与新规范的模块职责一致
    - _需求：2.3_

  - [x] 2.4 添加新的设计原则
    - 如果新规范引入了新的设计原则，添加到 README
    - _需求：2.5_

- [x] 3. 更新 STRUCTURE.md 文档
  - [x] 3.1 更新源码目录结构
    - 详细描述 `src/` 目录下的所有文件和子目录
    - 确保与新规范定义的结构完全一致
    - _需求：3.1_

  - [x] 3.2 更新模块化设计原则
    - 更新设计原则部分，与新规范保持一致
    - _需求：3.2_

  - [x] 3.3 更新文件职责说明
    - 详细描述每个文件的职责和功能
    - 确保与新规范的模块职责一致
    - _需求：3.3_

  - [x] 3.4 添加新的约束说明
    - 如果新规范定义了新的约束，添加到 STRUCTURE.md
    - _需求：3.5_

- [x] 4. Checkpoint - 文档更新完成
  - 确保所有文档更新完成，格式正确，内容准确
  - 人工审查更新后的文档
  - 如有问题，请向用户反馈

- [x] 5. 验证代码结构一致性
  - [x] 5.1 编写结构验证单元测试
    - 创建 `test/architecture-structure-validation.test.ts` 文件
    - 测试目录结构是否符合新规范（background、content、popup、shared、images）
    - 测试必需文件是否存在（index.ts、usage.ts、pro.ts、settings.ts、storage.ts 等）
    - 测试 shared/constants.ts 是否存在（新规范要求）
    - 测试文件命名是否符合规范
    - 测试模块依赖关系（content 不得 import background）
    - 使用 Jest 的断言记录发现的差异
    - _需求：4.1, 4.2, 4.3, 4.4, 4.5_

  - [ ]* 5.2 编写属性测试：结构验证正确性
    - **属性 4：结构验证正确性**
    - **验证：需求 4.1, 4.3, 4.5**
    - 生成随机的代码结构和规范，验证验证器的判断正确性
    - 最少 100 次迭代

- [x] 6. 验证代码实现一致性
  - [x] 6.1 编写实现验证单元测试
    - 创建 `test/architecture-implementation-validation.test.ts` 文件
    - 测试 Background 层是否只包含业务逻辑和状态管理
    - 测试 Content 层是否不包含业务逻辑判断
    - 测试 Content 层是否不直接访问 chrome.storage
    - 测试 Content 层是否不 import background 文件
    - 测试 Shared 层是否只包含类型定义和协议
    - 测试消息通信是否符合规范定义的协议格式
    - 使用 Jest 的断言记录发现的违规项
    - _需求：5.1, 5.2, 5.3, 5.4, 5.5_

  - [ ]* 6.2 编写属性测试：差异报告完整性
    - **属性 5：差异报告完整性**
    - **验证：需求 4.2, 4.4, 5.4**
    - 生成随机的不一致情况，验证验证器的报告完整性
    - 最少 100 次迭代

- [x] 7. 生成架构一致性报告
  - [x] 7.1 编写报告生成单元测试
    - 创建 `test/architecture-report-generation.test.ts` 文件
    - 测试报告生成功能
    - 汇总结构验证和实现验证的测试结果
    - 生成差异分析
    - 提供调整建议和实施步骤
    - 将报告保存到 `./docs/report/architecture-consistency-report.md`
    - 测试报告格式正确性和内容完整性
    - _需求：6.1, 6.2, 6.3, 6.4, 6.5_

  - [ ]* 7.2 编写属性测试：架构一致性报告完整性
    - **属性 6：架构一致性报告完整性**
    - **验证：需求 6.1, 6.2, 6.3, 6.5**
    - 生成随机的验证结果，验证报告的完整性和正确性
    - 最少 100 次迭代

- [ ] 8. Checkpoint - 验证和报告完成
  - 确保所有验证完成，报告生成成功
  - 人工审查生成的报告
  - 如有问题，请向用户反馈

- [ ] 9. 根据测试结果调整代码结构（如需要）
  - 根据单元测试中发现的问题
  - 调整不符合规范的代码结构（如缺少 shared/constants.ts）
  - 修复违反架构原则的代码实现
  - _需求：4.2, 4.4, 5.4_

- [ ] 10. 最终验证
  - 重新运行所有验证测试（`npm test`）
  - 确保所有架构验证测试通过
  - 更新架构一致性报告（如果有新的调整）

## 注意事项

- 任务标记 `*` 的为可选任务（测试相关），可以跳过以加快 MVP 开发
- 每个任务都引用了具体的需求编号，便于追溯
- Checkpoint 任务用于确保阶段性成果的质量
- 所有代码和注释使用中文
- 报告文件保存到 `./docs/report/` 目录
